# Packages
nano_pkg:
  pkg.installed:
    - name: nano

wget_pkg:
  pkg.installed:
    - name: wget

## SSH settings
# SSH keys
myssh_key:
  ssh_auth.present:
    - user: root
    - source: salt://ssh/id_rsa.pub
